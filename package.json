{
  "name": "gym-workout-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android wrapper for the GYM Workout Tracker web app.",
  "scripts": {
    "build": "node scripts/sync-web-assets.js",
    "sync:web": "node scripts/sync-web-assets.js",
    "cap:sync": "npm run sync:web && cap sync android",
    "cap:open": "cap open android",
    "android:debug": "npm run cap:sync && cd android && gradlew.bat assembleDebug",
    "android:release": "npm run cap:sync && cd android && gradlew.bat assembleRelease",
    "android:aab": "npm run cap:sync && cd android && gradlew.bat bundleRelease"
  },
  "dependencies": {
    "@capacitor-firebase/authentication": "7.3.0",
    "@capacitor/android": "^7.4.2",
    "@capacitor/app": "^7.1.2",
    "@capacitor/core": "^7.4.2",
    "@capacitor/haptics": "^7.0.2",
    "@capacitor/keyboard": "^7.0.2",
    "@capacitor/local-notifications": "^7.0.7",
    "@capacitor/splash-screen": "^7.0.2",
    "@capacitor/status-bar": "^7.0.2",
    "firebase": "^12.17.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.4.2"
  }
}
